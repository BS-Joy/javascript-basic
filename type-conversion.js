let age = "20";
console.log(typeof(age));
// now if we want to change the datatype of the variable age as number we can use parseInt() function.

age = parseInt(age);
console.log(typeof(age));

// convert a int as float

let num = 3;
num = parseFloat(num);
console.log(num.toFixed(2));